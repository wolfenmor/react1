const baseURL = "https://jsonplaceholder.typicode.com"

const todos = "/todos"
const albums = "/albums"

const urls = {
    todos:{
        base:todos
    },
    albums: {
        base:albums
    }
}

export {baseURL, urls}